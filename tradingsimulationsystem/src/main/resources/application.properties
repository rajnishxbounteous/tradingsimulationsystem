spring.application.name=tradingsimulationsystem

# --- Server Config ---
server.port=8080

# --- PostgreSQL Database Config ---
spring.datasource.url=jdbc:postgresql://localhost:5432/tradingdb
spring.datasource.username=postgres
spring.datasource.password=postgresql
spring.datasource.driver-class-name=org.postgresql.Driver

# --- JPA / Hibernate Config ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# --- Logging (optional, for debugging SQL) ---
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# --- JWT Config ---
# Added a secure Base64-encoded secret key for HS256.
# This must be at least 256 bits (32 bytes) when decoded.
# You can generate your own with:
#   java.util.Base64.getEncoder().encodeToString("your-long-random-string".getBytes())
jwt.secret=VGhpcy1pcy1hLXN1cGVyLXNlY3VyZS1rZXktdGhhdC1pcy1hdC1sZWFzdC0zMi1ieXRlcy1sb25nIQ==
